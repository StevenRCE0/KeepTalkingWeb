import { firebaseApp } from '@/work/firebase'
import styles from './page.module.css'


const handleSignal = () => {
  firebaseApp.
}

export default function Home() {
  return (
    <main className={styles.main}>
      <button>Signal</button>
    </main>
  )
}
